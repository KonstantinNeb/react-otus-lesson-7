import { LoginForm } from "./LoginForm"
import { BrowserRouter, Route, Routes } from "react-router-dom"
export default {
  title: "LoginForm",
  component: LoginForm,
}

//export const Basic = () => <LoginForm onSubmit={() => {}} />